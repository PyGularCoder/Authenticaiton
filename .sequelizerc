// .sequelizerc
const path = require('path');

module.exports = {
  'config': path.resolve('psql', 'config', 'database.js'), // Update to 'psql' if keeping it there
  'models-path': path.resolve('src', 'models'),
  'migrations-path': path.resolve('migrations'),
  'seeders-path': path.resolve('seeders'), // Optional
};